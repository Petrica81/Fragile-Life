using UnityEngine;

public class PlayerMovement:MonoBehaviour
{
    private void FixedUpdate()
    {
        float speed = 5f;

        if (Input.GetKeyDown(KeyCode.W))
        {
            //transform.position += 
            // addForce?
        }
    }
}
